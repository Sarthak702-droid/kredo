## Learned User Preferences

- Use bun for package management and dev scripts (e.g. `bun run dev`).
- Optimize for contest judges evaluating via GitHub README and live demo URL, not a guided pitch.
- Underwriter View should sync with the company dropdown (`selectedMsmeId`), not keep a separate fixed application selection.

## Learned Workspace Facts

- Kredo is an IDBI Innovate 2026 Track 03 submission (MSME Financial Health Card).
- GitHub repo: https://github.com/Sarthak702-droid/kredo; live demo: https://kredo.onrender.com.
- Firebase project `cellular-deck-471715-u1`; client config in `firebase-applet-config.json`.
- Local dev uses Admin Firestore only when `GOOGLE_APPLICATION_CREDENTIALS` (or cloud ADC) is set; otherwise `db.json` fallback.
- Stack: React 19 + Express/Vite, TypeScript, Tailwind CSS v4.
- Guided demo path centers on NTC MSME `msme-2` (Srinivasa Kirana) and lender case `app-2`.
